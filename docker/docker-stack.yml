version: '3.8'

services:
  yii2:
    image: yii2-app
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - web
    ports:
      - "8080:80"

networks:
  web:
    external: true
